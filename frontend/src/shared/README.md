# Shared frontend utilities

Shared UI and helper utilities.

Current:
- Toast store + host component (`toast.js`, `ToastHost.vue`).
- Lightweight i18n helper in `shared/i18n/` (English now, structured for future locales).
