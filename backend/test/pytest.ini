[tool:pytest]
testpaths = .
python_files = test_*.py
python_functions = test_*
python_classes = Test*
addopts = 
    -v
    --strict-markers
    --disable-warnings
    --tb=short
pythonpath = ..
asyncio_mode = auto
